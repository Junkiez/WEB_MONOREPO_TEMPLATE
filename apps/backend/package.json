{
  "name": "backend",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "dependencies": {
    "models": "workspace:*"
  },
  "devDependencies": {
    "nitropack": "latest"
  }
}
